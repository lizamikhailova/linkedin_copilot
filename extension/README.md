# Browser Helper

This scaffold is for a capture-only browser helper. The extension should:

- let the user explicitly capture a LinkedIn post while browsing
- extract visible post data
- send that payload to the local app API

It should not attempt to like, comment, or automate engagement actions.
